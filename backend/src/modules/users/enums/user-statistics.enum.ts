export enum TimeRangeType {
      ALL = 'all',
      YEAR = 'year',
      MONTH = 'month',
      DATE_RANGE = 'date_range',
}

export enum VerificationStatus {
      ALL = 'all',
      VERIFIED = 'verified',
      UNVERIFIED = 'unverified',
} 